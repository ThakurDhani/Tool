plugin: amazon.aws.aws_ec2
regions:
  - ap-south-1

filters:
  tag:Name: asg-instance

keyed_groups:
  - key: tags.Name
    prefix: tag_Name

hostnames:
  - instance_id

compose:
  ansible_host: public_ip_address

profile: default

